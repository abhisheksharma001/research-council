# First self-run — 2026-09-09

research-council pointed at its own checkout, following `skills/self-improve/SKILL.md` (S-22). Evidence-only, no promotion, no paid call, nothing read outside the repo. Run folder ignored by git.

## The problem
"Where is research-council weakest for a user with no memory of its design, and what one change fixes it." Budget from the user: 30 min, 100 actions, 4 subagents, $0. Criterion from the user: one proposed register step he accepts.

## What was run
| step | what happened | time (UTC) |
|---|---|---|
| goal, attempt 1 | `goal.py new` refused `usd_estimate_cap: 0` ("must be a number above 0"). Bug 9. Fixed as S-23 (PR #17) before the run, instead of inventing a number | 05:36 |
| goal, attempt 2 | frozen, revision 1; no S-16 warning (this repo ignores `AGI_Research/`) | 05:45:05 |
| evidence | 21 records from repo files and read-only commands: council.md fence text, tool fences, empty registry, first-run note rows, procedure size, which scripts read the journal, bug log cells, test state | 05:45–05:46 |
| claims | 14 claims, all with evidence | 05:46 |
| stage 1 | Generation (general-purpose agent, council.md fallback): H1..H5, I-1..I-6. Folder listing after: `hypotheses.json` only | 05:47–05:48 |
| stage 2 | Supervisor ran I-1, I-4, I-5, I-6 (6 records, 4 claims). `rank.py pair` drew P-1 = H2 vs H1. Reflection and Ranking spawned in parallel | 05:48–05:49 |
| P-1 | Ranking: B (H1, fencing) beat A (H2, library): 4 supporting claims vs 2, A "wider than its evidence". H1 1208, H2 1192 | 05:50 |
| objections | Reflection: 13 objections, 6 blocking. Seven of thirteen were the same pattern: an absence asserted from one grep instead of the file body | 05:51 |
| resolutions | 9 new records; 6 replacement claims C-19..C-24. Claims cannot be edited or superseded, so the originals stay in FINDINGS.md next to their replacements (bug 6, S-18, seen live) | 05:52 |
| meta-review | last subagent. Recommendation: continue for I-2 only (Supervisor-alone read), then stop | 05:53–05:54 |
| I-2 + report | 4 records, 3 claims. `report.py` → FINDINGS.md (121 lines), HANDOFF.md. Unverified: none | 05:54:46 |

Budget at close: `spent: 11/30 min, 54/100 actions, 4/4 subagents, ~$0.00/$0 (unmetered: 57)`. Wall clock goal to report: 10 minutes. Subagent tokens from task notifications: Generation 63,486; Ranking 59,633; Reflection 71,049; Meta-review 76,000.

## What the council found
- **Top-rated: H1, subagent fencing.** council.md says the run-folder listing after a spawn "is the fence", but no script performs it: outside `promote.py`, nothing in `scripts/` lists a directory (E-25, E-39). The first-run note has no row recording a listing after any spawn (E-37). In this run the listing was four `ls` calls and hand-written journal notes (E-40). H1's own stop condition is unmet on both halves (C-27).
- H2 (library path never exercised) lost P-1: true (registry empty, 22 promote tests on one synthetic fixture) but its statement was wider than its evidence, and the first-run note has no step-9 row either way (E-38).
- H3, H4, H5 (procedure, claim lifecycle, step 2) were not judged: no subagent left for a second pair. The bugs spread over step 2 (four), step 5 (one), the report and claims scripts (three) and meta-review (one) (C-21), so "one weakest step" is not supported.
- Bug 6 confirmed in the wild: six blocking objections needed six replacement claims, and the replaced claims still print as findings.
- `docs/bugs.md` row for bug 2 reads "S-14" without the PR number the other fixed rows carry, although commit aad21b2 touched the file (C-23).

## What it got wrong
- Same shape as the first run: 4 of 4 subagents gone before a second Reflection could check the replacement claims. Two runs, two early stops at the cap.
- Seven objections were about the Supervisor's own evidence style (absence from one grep). The council caught it; a lone model would not have.
- `rank.py pair --seed 1` drew H2 vs H1, not the two hypotheses the Supervisor thought strongest (H3, H5). With one pair per run, the seed decides what gets judged.

## Not done
No promotion. No library write. No paid call. No spark. No merge. The run folder stays under `AGI_Research/runs/`, ignored.

## Follow-up self-assessment — 2026-09-10

Source revision: 5abc1cf29eb9b006c8f0ba73f93af5b7bf7d2739, Python 3.11.15 on Darwin. Both skill validators and all 232 tests passed before changes. User selected agent apps first, deferred the Paperclip-specific connection, approved 60 minutes / 200 actions / 8 subagent launches / $0 additional paid API calls, and selected all offered success targets. External Astra documentation was reviewed separately before the repo-only run; it is not self-run evidence.

Run id: a9963323-9ff7-4a5e-9b63-549bc88626ab. The frozen goal, 12 evidence records, 9 claim records (including two replacements), journal, findings, and handoff remain in the ignored run folder. Close status: 16/60 minutes, 48/200 visible supervisor actions, 5/8 launches, $0 metered spend with 21 unmetered entries. This is not a full provider billing or internal worker-action measurement.

Generation, Reflection, Ranking, and Meta-review ran as read-only roles; Meta-review had one follow-up. P-1 was a draw between the install/runtime weakness and role authority weakness. C-6, the proposed live Paperclip/research-quality success claim, remained unverified. Reflection asked for missing environment/revision evidence; E-11 resolved the replacement claims C-7/C-8, and the originals were superseded rather than rewritten.

E-12/C-9 reproduced the portability issue: running the relative triage command from `skills/research-council/` exits 2 because its scripts are at the checkout root. E-4/E-5/E-6 show that the fallback's textual tool limits and run-folder hashes are not a filesystem sandbox. E-7/E-10 show two Markdown reports and an existing explicit distinction between scheduling and verification; no evidence supports calling the old report deceptive.

The council made mistakes too: Generation reversed the H3 predictions in two investigation branches, and Meta-review named a nonexistent supervisor role. The Supervisor checked the actual role inventory instead of following those paths. The corrected comparison added no discrimination, so Meta-review recommended stopping and moving to an engineering round-trip test.

This was an adapted run, not a clean execution of the original protocol. The editor denied writes to ignored files even after a scope grant. Existing validated record commands accepted stdin, and the Generation document used the existing persistence API, but Reflection and Meta-review replies remained summarized in the journal instead of objections.json/meta.md. The generated report correctly has no persisted review artifact, although its old fixed wording cannot distinguish an unsaved review from no review. S-33 addresses the missing result channel; this account does not claim an installed-host test passed.

Proposed engineering steps: S-32 portable runtime/export, S-33 read-only council result channel, S-34 structured handoff. No API calls, live Paperclip task writes, promotions, merges, or changes to invariants occurred during the run.
