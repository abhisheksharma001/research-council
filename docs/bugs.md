# Bug log

Format: date | where seen | what was seen | repro | spec line corrected | step queued

| date | where seen | what was seen | repro | spec line corrected | step queued |
|---|---|---|---|---|---|
| 2026-09-09 | `skills/research-council/references/goal.md` step 1-2; dry run step 2 | User did not answer the budget and success-criterion questions; the Supervisor copied both from `tests/fixtures/goal_booking.json` and wrote `"set_by": "user"` anyway. The procedure has no rule for a silent user, so "do not guess" was violated on the first real run and the HANDOFF acceptance line came out as the fixture's fields glued together. | Run SKILL.md step 2 with no user reply. Nothing stops `goal.py new` from accepting a fixture budget labelled `set_by: user`. | spec-v1.md S-13 (rule text for goal.md step 1 and SKILL.md step 2) | S-13 |
| 2026-09-09 | `skills/research-council/references/council.md`; dry run step 5 | "Spawn Reflection" has no mechanism. In a session where the plugin is a checked-out repo, not an installed plugin, the four `agents/*.md` roles are not registered as subagent types, so the Supervisor spawned general-purpose agents and told each to read its role file. The `tools:` fence in the role frontmatter is then advisory, not enforced. | `ls agents/` shows four roles; the Agent tool lists none of them. Spawning with subagent type `reflection` is refused. | spec-v1.md S-14 (fallback text for council.md "Every spawn") | S-14 |
| 2026-09-09 | `scripts/retrieve.py` `tokens()`; dry run step 2 | Bare numbers count as query tokens: `on Sep 04 774 calls failed` tokenises to `04 774 calls failed sep`. A skill whose mechanism mentions any number shares a token by accident. | `python3 scripts/retrieve.py --query "on Sep 04 774 calls failed" --library library` prints `query tokens: 04 774 calls failed sep`. | spec-v1.md S-15 (token rule for retrieve.md) | S-15 |
| 2026-09-09 | `skills/research-council/SKILL.md` step 2; `scripts/goal.py new`; dry run | The run folder lives under the user's workspace and fills with client data (prompt excerpts, names, phone numbers). Nothing tells the Supervisor to keep it out of git. The client workspace had no ignore rule for `AGI_Research/` and the folder showed up as untracked. | In any git workspace without an ignore rule run step 2, then `git status --short` shows `?? AGI_Research/`. | spec-v1.md S-16 (goal.py new warns; SKILL.md step 2 says to add the ignore rule) | S-16 |
| 2026-09-09 | `scripts/report.py`; council.md stage 2 step 2; dry run report | council.md says a `blocking: true` objection keeps its claim out of FINDINGS.md until new evidence resolves it. report.py never opens `objections.json`; C-6 (blocking objection O-5, unresolved) rendered under "What we found". | `grep -n objections scripts/report.py` prints nothing. Render any run with an `objections.json` naming a claim as blocking: the claim appears under What we found. | spec-v1.md S-17 (report.py reads objections.json, Disputed section) | S-17 |
| 2026-09-09 | `scripts/claims.py`; dry run round 2 | A claim cannot be corrected once written. C-5 ("the only attempt after the block") and C-11 (wrong arithmetic) were corrected by C-16 and C-15 but still render as findings beside their corrections. `claims.py` has only `add` and `list`. | `python3 scripts/claims.py -h` lists `{add,list}`. Add two contradicting claims with evidence; both render under What we found. | spec-v1.md S-18 (claims.py supersede, report.py drops superseded) | S-18 |
| 2026-09-09 | `scripts/report.py` line 130; dry run HANDOFF.md | "Beat: H3" was printed although H2 was only ever compared with H1. The line takes `ranked[1]`, the Elo runner-up, regardless of `comparisons.jsonl`. | Render `tests/fixtures/run_min` after one recorded pair that does not involve the second-ranked hypothesis: Beat names the runner-up anyway. | spec-v1.md S-19 (Beat names a judged opponent or nothing) | S-19 |
| 2026-09-09 | `agents/meta-review.md`, `references/council.md`; dry run round 1 | Meta-review asked for H1 and H4 to be marked stopped (O-11, O-12). `hypotheses.json` is Generation-only under the fence and no script sets a hypothesis status, so both stayed `open` in the Elo table and `rank.py pair` would still draw them. | Read meta.md from any run whose Reflection reported `stop_conditions_met`; check `hypotheses.json`: status unchanged. `python3 scripts/rank.py -h` has no stop command. | spec-v1.md S-20 (rank.py stop) | S-20 |

All eight found in the first dry run: `docs/runs/2026-09-09-first-run.md`.
