# Bug log

Format: date | where seen | what was seen | repro | spec line corrected | step queued

| date | where seen | what was seen | repro | spec line corrected | step queued |
|---|---|---|---|---|---|
| 2026-09-09 | `skills/research-council/references/goal.md` step 1-2; dry run step 2 | User did not answer the budget and success-criterion questions; the Supervisor copied both from `tests/fixtures/goal_booking.json` and wrote `"set_by": "user"` anyway. The procedure has no rule for a silent user, so "do not guess" was violated on the first real run and the HANDOFF acceptance line came out as the fixture's fields glued together. | Run SKILL.md step 2 with no user reply. Nothing stops `goal.py new` from accepting a fixture budget labelled `set_by: user`. | spec-v1.md S-13 (rule text for goal.md step 1 and SKILL.md step 2) | S-13 — fixed PR #12 |
| 2026-09-09 | `skills/research-council/references/council.md`; dry run step 5 | "Spawn Reflection" has no mechanism. In a session where the plugin is a checked-out repo, not an installed plugin, the four `agents/*.md` roles are not registered as subagent types, so the Supervisor spawned general-purpose agents and told each to read its role file. The `tools:` fence in the role frontmatter is then advisory, not enforced. | `ls agents/` shows four roles; the Agent tool lists none of them. Spawning with subagent type `reflection` is refused. | spec-v1.md S-14 | S-14 — fixed PR #13 |
| 2026-09-09 | `scripts/retrieve.py` `tokens()`; dry run step 2 | Bare numbers count as query tokens: `on Sep 04 774 calls failed` tokenises to `04 774 calls failed sep`. A skill whose mechanism mentions any number shares a token by accident. | `python3 scripts/retrieve.py --query "on Sep 04 774 calls failed" --library library` prints `query tokens: 04 774 calls failed sep`. | spec-v1.md S-15 (token rule for retrieve.md) | S-15 — fixed PR #14 |
| 2026-09-09 | `skills/research-council/SKILL.md` step 2; `scripts/goal.py new`; dry run | The run folder lives under the user's workspace and fills with client data (prompt excerpts, names, phone numbers). Nothing tells the Supervisor to keep it out of git. The client workspace had no ignore rule for `AGI_Research/` and the folder showed up as untracked. | In any git workspace without an ignore rule run step 2, then `git status --short` shows `?? AGI_Research/`. | spec-v1.md S-16 (goal.py new warns; SKILL.md step 2 says to add the ignore rule) | S-16 — fixed PR #15 |
| 2026-09-09 | `scripts/report.py`; council.md stage 2 step 2; dry run report | council.md says a `blocking: true` objection keeps its claim out of FINDINGS.md until new evidence resolves it. report.py never opens `objections.json`; C-6 (blocking objection O-5, unresolved) rendered under "What we found". | `grep -n objections scripts/report.py` prints nothing. Render any run with an `objections.json` naming a claim as blocking: the claim appears under What we found. | spec-v1.md S-17 (report.py reads objections.json, Disputed section) | S-17 |
| 2026-09-09 | `scripts/claims.py`; dry run round 2 | A claim cannot be corrected once written. C-5 ("the only attempt after the block") and C-11 (wrong arithmetic) were corrected by C-16 and C-15 but still render as findings beside their corrections. `claims.py` has only `add` and `list`. | `python3 scripts/claims.py -h` lists `{add,list}`. Add two contradicting claims with evidence; both render under What we found. | spec-v1.md S-18 (claims.py supersede, report.py drops superseded) | S-18 — fixed PR #21 |
| 2026-09-09 | `scripts/report.py` line 130; dry run HANDOFF.md | "Beat: H3" was printed although H2 was only ever compared with H1. The line takes `ranked[1]`, the Elo runner-up, regardless of `comparisons.jsonl`. | Render `tests/fixtures/run_min` after one recorded pair that does not involve the second-ranked hypothesis: Beat names the runner-up anyway. | spec-v1.md S-19 (Beat names a judged opponent or nothing) | S-19 — fixed PR #27 |
| 2026-09-09 | `agents/meta-review.md`, `references/council.md`; dry run round 1 | Meta-review asked for H1 and H4 to be marked stopped (O-11, O-12). `hypotheses.json` is Generation-only under the fence and no script sets a hypothesis status, so both stayed `open` in the Elo table and `rank.py pair` would still draw them. | Read meta.md from any run whose Reflection reported `stop_conditions_met`; check `hypotheses.json`: status unchanged. `python3 scripts/rank.py -h` has no stop command. | spec-v1.md S-20 (rank.py stop) | S-20 — fixed PR #28 |
| 2026-09-09 | `scripts/goal.py` line 109; self-run goal capture | `goal.py new` refused `"usd_estimate_cap": 0` with `must be a number above 0`, so a run that may spend nothing could not be started | set the cap to 0 in `tests/fixtures/goal_booking.json` and run `goal.py new` | `references/goal.md` before-writing step 1 | S-23 — fixed PR #17 |
| 2026-09-10 | `scripts/report.py` `handoff()`; found while shipping S-20 | Chosen is the top-rated hypothesis with any status but `refuted`, so a hypothesis stopped by `rank.py stop` can be printed as `Chosen:` in HANDOFF.md. | On `tests/fixtures/run_min` run `rank.py stop --hyp H2 --reason O-1` then `report.py --run <copy>`: HANDOFF.md still reads `Chosen: H2`. | spec-v1.md S-29 (Chosen must be `open`) | S-29 — fixed PR #30 |

Bugs 1-8 found in the first dry run: `docs/runs/2026-09-09-first-run.md`. Bug 9 found by the first self-run (S-22). Bug 10 found while shipping S-20.

## 2026-09-10 portability self-run

| id | evidence and reproduction | correction | state |
|---|---|---|---|
| 11 | E-2/E-3/E-12, C-7/C-9: the skill folder contains only SKILL.md and references. Invoking `python3 scripts/triage.py --answers -` with the fixed self-improve answers from `skills/research-council/` exits 2 because the helper is at the checkout root. A full checkout and absolute helper path work around this; no claim that every other host fails. | S-32: explicit runtime/workspace roots, portable export, and a foreign-working-directory test. | fixed locally; 15 harness tests, including a path-guard break test |
| 12 | E-4/E-5/E-6/E-9, C-2/C-3: Generation calls its tool list the only limit; the fallback admits that list is unenforced. The fence hashes only run files, excluding journal/fence; it cannot prevent outside writes. The static test preserves the misleading wording. | S-33: return-only roles, validated result ingestion, and explicit host-enforced permissions. Never delete a violating file automatically. | fixed locally; procedure and runtime tests |
| 13 | During this host run the editor refused ignored-file writes even after a directory scope grant. Existing goal/evidence/claims commands accepted stdin, but Reflection and Meta-review had no equivalent ingestion command. Their replies stayed in the journal rather than objections.json/meta.md. This is a host integration limitation, not a permission bypass to implement. | S-33: a supervisor-owned command accepts validated role replies through stdin, without giving workers write access. | fixed locally in S-33; regression-tested |
| 14 | Independent code review: `journal.parse_cost("nan")` passes the negative-number check; the resulting total is NaN and comparisons against dollar caps are false. The direct journal API also accepts invalid numeric values. `goal.validate` accepts non-finite caps, allowing a NaN revision to evade the no-raise comparison. | S-35: reject invalid/non-finite caps and costs on write and when reading legacy records; never treat invalid or missing cost data as free. | fixed locally; 47 goal/budget tests; finite-value guards proved by removal |
| 15 | Review of the new S-33 result channel found journal.jsonl omitted from its record-symlink check. A link to an empty file outside the run could be appended through the journal helper. The host must still enforce worker permissions, but the controller should reject this path alias itself. | S-33: include the journal in path checks, without including legitimate journal updates in the frozen input fingerprint. | fixed locally in S-33; regression-tested |
| 20 | S-33 draft tests: a reply containing an invalid Unicode surrogate passed structural checks and truncated the fixture's previous meta.md before UTF-8 encoding failed. A simulated replacement failure also lacked a preservation boundary. Only temporary test data was affected. | S-33: encode before touching the output, write to a private temporary file, and atomically replace the generated result; clean up only that newly created temporary file. | fixed locally in S-33; regression-tested |
| 21 | S-34 tests: the report reader accepts an object instead of an objections list and accepts numeric 1 as a boolean-shaped value, then silently treats the review as non-blocking. Neither input produces the malformed-review warning. Final review also found unknown claim references warned about in JSON but silently ignored in Markdown. | S-34: share review-container, exact-boolean and claim-reference validation; structured output keeps backed claims unreviewed when review records are missing or invalid. | fixed locally; 46 report tests and guard-removal checks |

## 2026-09-17 coding-agents research run

| id | evidence and reproduction | correction | state |
|---|---|---|---|
| 16 | `rank.py pair --seed 1` then `rank.py pair --seed 2` before recording the first issued P-1 and P-2 with the same two hypotheses: `pair` excludes recorded opponents, not issued-but-unrecorded ones. Judging both would double-count one matchup. P-2 was left unjudged and journaled. | `pair` reads `pairs.jsonl` and refuses to reissue a matchup that has no record yet; rank.md says one pair at a time. | queued |
| 17 | `fence.py check --role ranking` printed `violation: ranking wrote pairs.jsonl`. The change was the Supervisor's own `rank.py pair` run during the spawn; the role has no write path. The fence cannot tell a Supervisor write from a role write and council.md never says not to run writing scripts while a role is out. | council.md: no run-folder-writing script while a role is spawned; fence ignores `pairs.jsonl` and `comparisons.jsonl`, which only the Supervisor's scripts write. | queued |
| 18 | `budget.py check` printed `313/90 min` at report time after 25 minutes of Supervisor work; the run waited 288 minutes between session turns. Minutes are wall clock since `created_at` (budget.md), so an interactive pause consumes the user's cap and forces exit 2 on the next check. | budget.md states the wall-clock rule up front and SKILL.md tells the Supervisor to finish a run in one sitting or report the pause; an active-time line from journal timestamps beside the wall-clock line. | queued |
| 19 | Reflection's reply was 15 KB of JSON with raw control characters inside strings; the task notification showed only its tail, and `json.loads` refused it until parsed with `strict=False`. council.md says "save its reply as objections.json" and gives no ingestion command (bug 13 again, seen from the other side). | S-33's validated return-only ingestion command, with lenient parsing and shape validation, covers it. | queued under S-33 |


## 2026-09-21 S-45 marketplace step

| id | evidence and reproduction | correction | state |
|---|---|---|---|
| 22 | `grep -n "not pushed or merged" docs/spec-v1.md` prints the four Upgrade status rows (S-32, S-35, S-33, S-34), and README's Status paragraph says the 0.2.0 candidate "is not published, merged". `git log origin/main --oneline` shows all four merged: 7c71c3f (#36), 91e83f5 (#37), 5c22b4f (#38), cfde0ee (#41). The register and README claim less than what is true, so a reader cannot trust either for what is on main. | Rows and README paragraph state the merge with PR number; docs only. | fixed 2026-09-21 in S-46 (PR #57) |

## 2026-09-21 jev seam design pass

Found while mapping every closed-answer judgement in both councils for `docs/spec-jev.md`.
Rows 23, 24 and 25 change a verdict a reader would act on; 26 to 29 are queued behind the seam.

| id | evidence and reproduction | correction | state |
|---|---|---|---|
| 23 | `scripts/report.py` `_review_records` wraps the objection loop in `except (ValueError, KeyError, TypeError)` and returns the fixed note `bad_objections_file`. One bad objection therefore discards the whole file: in the five runs on this machine that would drop 62 objections, 23 of them blocking. The JSON handoff then moves every backed claim to `unreviewed` and empties `findings`, but FINDINGS.md still prints those claims under "What we found", disputed ones included. Repro: put one objection with `"blocking": "true"` (the string) in objections.json and run `report.py`. `scripts/council.py` rejects the same input instead, so one file has two behaviours. | FINDINGS.md prints those claims under "Unreviewed (objections file unreadable)" with the note, leaving "What we found" empty, which is what the JSON already says; the note names the first offending objection and the reason. | fixed 2026-09-21 in S-48 (PR #50): `scripts/report.py` `_review_records` now names the first objection it could not read, and `findings()` moves backed claims to "Unreviewed (objections file unreadable)" so FINDINGS.md and the JSON agree |
| 24 | `scripts/done.py` `check` runs the frozen test command and writes its `command` evidence record and `exec` journal line before it parses review-<n>.json, thinker.json and resolutions.jsonl. A finding whose severity is neither `blocking` nor `advisory` raises there, so the check exits 1 with no NOT DONE line after a full test run and one metered action are already spent. Repro: put `"severity": "critical"` in review-1.json and run `done.py check`. | Read the three files before the test run; a malformed file still exits 1, but before any test run, evidence record or journal line. | fixed 2026-09-21 in S-49 (PR pending): `check` calls `findings`, `resolutions` and `thinker_tests` straight after the guard block, above the test run, so a file that does not parse still exits 1 but spends no test run, evidence record or journal line |
| 25 | `scripts/done.py` validates a `fixed` resolution's `diff_sha` against `^[0-9a-f]{64}$` and never compares it with the diff it is about to call done. An edit that undoes the fix still passes, with the old resolution line closing the finding. Rule 7 of the code-writer-council says a blocking finding is closed by a fix with a new diff sha. Repro: `done.py resolve --fixed`, then edit any tracked file, then `done.py check`: DONE. | Compare a fixed resolution's sha with the sha computed before the test run and print `stale resolution: <id>` when they differ. Whether this is a defect or intended is R-7 in `docs/research.md`. | fix queued as S-50 in `docs/spec-jev.md`, behind R-7 |
| 26 | `scripts/done.py` decides a Thinker test exists by asking whether its declared name appears anywhere in the added text, and the added text includes every untracked file in full. A comment, a docstring or an unrelated new file that merely mentions the name satisfies the check. | Match the name as a definition in a verifier file, not as a substring of arbitrary added text. | fixed 2026-09-22 in S-58 (PR #58) |
| 27 | `scripts/scope.py` looks for weakening markers with a lowercased substring test, so `skip` matches `skipped` and `skipping` and any identifier containing it; and its comment prefixes include `*` and `--`, so a removed SQL line starting `--`, or a wrapped line starting `*`, never counts as a real removed line in a verifier file. | Match markers as tokens, and make the comment prefixes depend on the file's language. | comment prefixes fixed 2026-09-22 in S-59 (PR #TBD); marker matching queued as S-60 |
| 28 | `scripts/council.py` requires each meta-review heading to appear exactly once as `## <name>\n` and reads the recommendation's first whitespace token case-sensitively, so `Continue`, `Stop.` or a bolded heading refuses the whole reply; `scripts/rank.py` compares the winner against `A`, `B`, `draw` case-sensitively. A reply worth thousands of tokens is discarded for a capital letter, and the reserved spawn is not refunded. | A lossless normalisation pass before validation, journaled as a note: S-57 in `docs/spec-jev.md`. | queued as S-57 |
| 29 | `CLAUDE.md` names `~/AGI_Research/` as the research folder, but every run on this machine lives at `<repo>/AGI_Research/runs/<goal_id>/`, and `~/AGI_Research/` is an unrelated bibliography corpus. Separately, `docs/runs/2026-09-09-self-run.md` quotes 21 evidence records and 14 claims where the run folder holds 40 and 27, because the prose was written at a mid-run checkpoint; its appended 2026-09-10 section quotes 9 claims against 11 on disk. | Correct the path in `CLAUDE.md`, and mark the two counts in the run note as mid-run checkpoints. | queued (docs) |
| 30 | Same split as bug 23, on the other branch: a **missing** objections.json makes `structured_handoff` empty `findings` and mark every backed claim `unreviewed`, while FINDINGS.md still prints those claims under "What we found". Both behaviours are pinned on purpose (`tests/test_report.py` `test_missing_objections_file_is_one_fixed_line` and `test_json_missing_review_keeps_backed_claims_unreviewed`), so unlike bug 23 this may be intended: a run with no Reflection yet is a known absence, not an unknown one. Repro: delete objections.json from a run and compare `report.py --run <run>` with `report.py --run <run> --json`. | Decide whether the Markdown should say "Unreviewed (no Reflection review saved)" the way S-48 made the unreadable case say it, or whether the JSON should keep listing them as findings. Needs Abhishek, because both sides are deliberate. | open; found while shipping S-48 |
