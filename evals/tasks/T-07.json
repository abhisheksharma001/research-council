{
  "id": "T-07",
  "kind": "golden",
  "request": "test_report_dates fails on CI about once a day and never locally. Is the CI machine flaky or is the test wrong?",
  "known_answer": "test_report_dates compares date.today() in local time with a UTC timestamp; CI runs in UTC+5:30 and fails between 00:00 and 05:30 local, when the two dates differ. The test is wrong, not the machine.",
  "source": "synthetic: the cause is fixed by construction in this task's files; no real system or client",
  "files": {
    "ci-failures.txt": "run 4411 2026-09-20T18:42Z FAIL test_report_dates (runner TZ=Asia/Kolkata)\nrun 4415 2026-09-21T19:05Z FAIL test_report_dates\nrun 4418 2026-09-22T09:10Z PASS\n",
    "test_reports.py": "def test_report_dates():\n    r = make_report()\n    assert r.created.date() == datetime.date.today()  # r.created is UTC\n",
    "runner.md": "CI runners: 4 vCPU, TZ=Asia/Kolkata, retries disabled. No other test fails.\n"
  },
  "rubric": [
    {
      "id": "R1",
      "text": "names the time zone or UTC",
      "match": "time ?zone|\\butc\\b|tz"
    },
    {
      "id": "R2",
      "text": "names the local date against UTC comparison",
      "match": "date\\.today|today\\(\\)|local (time|date)|midnight|05:30|kolkata"
    }
  ]
}
