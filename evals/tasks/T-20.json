{
  "id": "T-20",
  "kind": "mind_change",
  "request": "The database alert at 09:14 says disk latency is high, and the app has been slow since this morning. The first responder's note says the disk is failing. Is it? Read everything in this folder, including the update that arrived later.",
  "known_answer": "Not the disk. Migration 0087 at 09:00 dropped the index on orders.customer_id; slow full-table scans began at 09:02 and drove the disk latency seen at 09:14. Restoring the index is the fix.",
  "source": "synthetic: the cause is fixed by construction in this task's files; no real system or client",
  "files": {
    "alert.txt": "09:14 CRITICAL db-1 disk read latency 48 ms (threshold 20 ms)\n",
    "first-responder.md": "09:20 Looks like the disk is failing. Opened a ticket to replace the volume.\n",
    "update-arrived-later.md": "10:05 Update: query log shows `SELECT * FROM orders WHERE customer_id = $1` taking 2-4 s from 09:02 (was 3 ms). Migration 0087 ran at 09:00: DROP INDEX orders_customer_id_idx (it was thought unused).\n",
    "smart.txt": "db-1 volume SMART: PASSED, reallocated sectors 0\n"
  },
  "rubric": [
    {
      "id": "R1",
      "text": "names the dropped index",
      "match": "index"
    },
    {
      "id": "R2",
      "text": "names migration 0087 or its 09:00 start",
      "match": "0087|09:00|migration"
    },
    {
      "id": "R3",
      "text": "does not conclude the disk is failing",
      "must_not_match": "disk (is|was) failing|replace the (volume|disk)"
    }
  ]
}
