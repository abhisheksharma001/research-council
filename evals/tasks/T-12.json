{
  "id": "T-12",
  "kind": "golden",
  "request": "The log server's disk filled up overnight. Is the log volume real traffic or a config mistake?",
  "known_answer": "On 2 September the app log level was set to DEBUG, and logrotate only matches *.log.1 patterns for app.log, so debug.log (180 GB) is never rotated. Traffic is normal.",
  "source": "synthetic: the cause is fixed by construction in this task's files; no real system or client",
  "files": {
    "du.txt": "180G /var/log/app/debug.log\n2.1G /var/log/app/app.log\n",
    "config-history.md": "2026-09-02: LOG_LEVEL INFO -> DEBUG (to chase a bug; revert later)\n",
    "logrotate.conf": "/var/log/app/app.log {\n  daily\n  rotate 7\n  compress\n}\n",
    "traffic.md": "Requests/day flat at ~2.1M for a month.\n"
  },
  "rubric": [
    {
      "id": "R1",
      "text": "names the debug log level or debug.log",
      "match": "debug"
    },
    {
      "id": "R2",
      "text": "names logrotate or rotation",
      "match": "rotat"
    }
  ]
}
