{
  "id": "T-04",
  "kind": "golden",
  "request": "The report worker's memory grows until it is killed every six hours. Two changes went out last week: a new image library and a response cache. Which one leaks, and how do you know?",
  "known_answer": "The response cache added in commit a1b2c3d has no eviction; ResponseCache retains 1.2 GB in the heap summary and grows with every request. The image library holds 14 MB and is flat.",
  "source": "synthetic: the cause is fixed by construction in this task's files; no real system or client",
  "files": {
    "heap-summary.txt": "retained by class after 5h40m:\n  ResponseCache._store (dict)   1.21 GB   entries 2,904,112\n  PIL.ImageFile buffers         14 MB\n  other                          88 MB\n",
    "git-log.txt": "a1b2c3d add ResponseCache to report worker (unbounded dict, eviction TODO)\n9f8e7d6 switch thumbnails to Pillow-SIMD\n",
    "heap-2h.txt": "ResponseCache._store 0.43 GB; PIL buffers 14 MB\n"
  },
  "rubric": [
    {
      "id": "R1",
      "text": "cites the 1.2 GB the cache retains",
      "match": "1\\.2\\d? ?gb"
    },
    {
      "id": "R2",
      "text": "names commit a1b2c3d or the missing eviction",
      "match": "a1b2c3d|evict|unbounded"
    }
  ]
}
